
Ubuntu: sudo apt install git make stow curl

MacPorts: sudo port install stow curl



git clone URL_OF_THIS_REPO
cd dotfiles-public
make


Emacs:   M-x package-install-selected-packages

Vim:     :PlugInstall

Git:     Set name + email in ~/dotfiles/gitconfig_user


